"""Web (headless browser) connector source package.

Tools live in `browser.py` and are re-exported via `connector.py` so
the embedded MCP can import them through the entrypoint declared in
the sibling `connector.yaml`.
"""
