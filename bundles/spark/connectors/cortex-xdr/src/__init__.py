"""Cortex XDR connector — v0.5.61 (issue #36).

Wraps the Cortex XDR Public API (/public_api/v1/...) for incident
listing + XQL query execution. The agent's evidence surface for
incident investigations: pull cases and alerts, drill into a case,
query the XDR data lake for host/user activity.
"""
