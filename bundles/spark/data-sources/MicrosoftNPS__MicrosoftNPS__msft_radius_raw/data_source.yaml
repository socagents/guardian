schema_version: 1
id: MicrosoftNPS
pack_name: MicrosoftNPS
rule_name: MicrosoftNPS
dataset_name: msft_radius_raw
vendor: Microsoft
product: MicrosoftNPS
description: Modeling Rules for the Microsoft NPS logs collector
categories:
- Analytics & SIEM
version: 1.0.12
origin: bundle
author: phantom-bundle
uploaded_by: null
created_at: '2026-05-24T16:53:58Z'
updated_at: '2026-05-24T16:53:58Z'
logo:
  mime_type: image/svg+xml
  data: 
  source: simpleicons:microsoft
  license: CC0-1.0
  fidelity: monochrome-brand
formats:
- SYSLOG
- CEF
- JSON
is_rawlog_only: true
fields:
- name: _raw_log
  type: string
  description: Raw log line as the vendor emits it before any parsing or normalization.
    Free-form text; may contain whitespace and punctuation.
  example: '<14>2024-01-15T14:23:01+0000 microsoft: microsoftnps=MicrosoftNPS action=allow
    user=jdoe'
use_cases:
- Cloud
- SaaS
- Identity
how_to_use: |
  ## Microsoft MicrosoftNPS → Cortex XSIAM (dataset `msft_radius_raw`)
  
    **Routing** — the broker lowercases the CEF/syslog header `vendor`/`product`, replaces
    non-alphanumerics with `_`, and appends `_raw` to choose the dataset (target: `msft_radius_raw`).
  
    **Required CEF header for XSIAM** — the header must normalize to `msft_radius`. The marketplace pack's display name `Microsoft` / `MicrosoftNPS` normalizes to `microsoft_microsoftnps_raw`, a **different dataset** (vendor rename / legacy CEF identity), so use the vendor's actual CEF `deviceVendor`/`deviceProduct` that yields `msft_radius` and confirm which dataset the events land in.
  
    **Ingestion shape**: **Flat / CEF-compatible.** The modeling rule reads flat top-level columns (no nested-JSON access). A CEF event routes in and the broker auto-extracts those columns, so this source maps from Phantom's existing CEF path once the dataset is XDM-bound.
  
    - **Modeling-rule gate**: none — the rule maps every routed row unconditionally.
  
    **XDM mapping**: ~8 fields mapped to the unified data model (`xdm.*`), including: `XDM.Auth.Client.host.ipv4_addresses`, `XDM.Auth.Client.port`, `XDM.Auth.Client.user.username`, `XDM.Auth.Server.host.device_id`, `XDM.Auth.Server.host.hostname`, `XDM.Auth.event_timestamp`.
  
    **Operator setup**: This data source uses `_raw_log`-based parsing. Cortex's parsing rule reads from `_raw_log` to populate `_json` (or directly to extract XDM fields), and the modeling rule does `regextract`/`json_extract_scalar` on `_raw_log`. For simulated logs sent from Phantom to land in this dataset, **the operator must add a Broker VM Syslog Applet pre-configured with the matching vendor + product + a dedicated source port**. Without that applet, logs are tagged `unknown/unknown` and routed to `unknown_unknown_raw` — the parsing rule never fires. 
  
  Setup: XSIAM → Settings → Configurations → Data Broker → Applets → Add Applet (Syslog) → Vendor = `Microsoft`, Product = `MicrosoftNPS`, Port = (operator picks an unused port, e.g. 1514, 1515, ...). Phantom must then send to that port — current default is 514, which is reserved for the ASA applet.
  
    **Status**: Reverse-engineered 2026-06-04 from the parsing/modeling rules — routing, gate, and field
    inventory documented. **NOT yet XDM-validated on a live tenant** (no `validated` pill); validate by
    streaming a CEF batch and confirming a rich `xdm.*` count over a wide window.
  
    **Verify** (once the dataset is XDM-bound, wide ≥7d window):
  
        config timeframe = 30d | datamodel dataset = msft_radius_raw | sort desc _time | fields xdm.* | limit 20
