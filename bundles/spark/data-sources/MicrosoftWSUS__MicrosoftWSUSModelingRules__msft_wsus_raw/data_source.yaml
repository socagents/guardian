schema_version: 1
id: MicrosoftWSUS
pack_name: MicrosoftWSUS
rule_name: MicrosoftWSUSModelingRules
dataset_name: msft_wsus_raw
vendor: Microsoft
product: MicrosoftWSUS
description: Modeling Rules for the Microsoft WSUS logs collector
categories:
- Analytics & SIEM
version: 1.0.11
origin: bundle
author: phantom-bundle
uploaded_by: null
created_at: '2026-05-24T16:53:58Z'
updated_at: '2026-05-24T16:53:58Z'
logo:
  mime_type: image/svg+xml
  data: 
  source: simpleicons:microsoft
  license: CC0-1.0
  fidelity: monochrome-brand
formats:
- SYSLOG
- CEF
- JSON
is_rawlog_only: false
fields:
- name: _raw_log
  type: string
  description: Raw log line as the vendor emits it before any parsing or normalization.
    Free-form text; may contain whitespace and punctuation.
  example: '<14>2024-01-15T14:23:01+0000 microsoft: microsoftwsus=MicrosoftWSUS action=allow
    user=jdoe'
- name: EventId
  type: string
  description: Event ID. Free-form text; may contain whitespace and punctuation.
  example: evt-942104bf
- name: Message
  type: string
  description: Event description. Free-form text; may contain whitespace and punctuation.
  example: Operation completed successfully.
- name: action
  type: string_short
  description: Event outcome reason. Short opaque token, typically without whitespace;
    max ~64 chars.
  example: token
- name: columns
  type: string
  description: Columns. Free-form text; may contain whitespace and punctuation.
  example: sample_columns
- name: event_category
  type: string
  description: Event type. Free-form text; may contain whitespace and punctuation.
  example: access_control
- name: event_time
  type: datetime
  description: Event time. ISO-8601 timestamp with timezone (e.g., 2026-05-26T14:23:01Z).
  example: '2026-05-26T14:23:01Z'
- name: service_name
  type: string
  description: Destination process name. Free-form text; may contain whitespace and
    punctuation.
  example: sample_name
- name: severity
  type: string_short
  description: Alert severity. Short opaque token, typically without whitespace; max
    ~64 chars.
  example: token
use_cases:
- Cloud
- SaaS
- Identity
how_to_use: |
  ## MSFT WSUS → Cortex XSIAM (dataset `msft_wsus_raw`)
  
    **Routing** — the broker lowercases the CEF/syslog header `vendor`/`product`, replaces
    non-alphanumerics with `_`, and appends `_raw` to choose the dataset (target: `msft_wsus_raw`).
  
    **Required CEF header for XSIAM**:
    - **vendor**: `MSFT`
    - **product**: `WSUS`
  
    These normalize → `msft_wsus_raw`.
  
    **Ingestion shape**: **Flat / CEF-compatible.** The modeling rule reads flat top-level columns (no nested-JSON access). A CEF event routes in and the broker auto-extracts those columns, so this source maps from Phantom's existing CEF path once the dataset is XDM-bound.
  
    - **Modeling-rule gate**: none — the rule maps every routed row unconditionally.
  
    **XDM mapping**: ~13 fields mapped to the **Endpoint** data model (`XDM.Endpoint.*`), including: `XDM.Endpoint.Target.process.name`, `XDM.Endpoint.event_timestamp`, `XDM.Endpoint.original_event_description`, `XDM.Endpoint.original_event_id`, `XDM.Endpoint.original_event_type`, `XDM.Endpoint.reason`.
  
    **Operator setup**: This data source uses `_raw_log`-based parsing. Cortex's parsing rule reads from `_raw_log` to populate `_json` (or directly to extract XDM fields), and the modeling rule does `regextract`/`json_extract_scalar` on `_raw_log`. For simulated logs sent from Phantom to land in this dataset, **the operator must add a Broker VM Syslog Applet pre-configured with the matching vendor + product + a dedicated source port**. Without that applet, logs are tagged `unknown/unknown` and routed to `unknown_unknown_raw` — the parsing rule never fires. 
  
  Setup: XSIAM → Settings → Configurations → Data Broker → Applets → Add Applet (Syslog) → Vendor = `MSFT`, Product = `WSUS`, Port = (operator picks an unused port, e.g. 1514, 1515, ...). Phantom must then send to that port — current default is 514, which is reserved for the ASA applet.
  
    **Status**: Reverse-engineered 2026-06-04 from the parsing/modeling rules — routing, gate, and field
    inventory documented. **NOT yet XDM-validated on a live tenant** (no `validated` pill); validate by
    streaming a CEF batch and confirming a rich `XDM.Endpoint.*` count over a wide window.
  
    **Verify** (once the dataset is XDM-bound, wide ≥7d window):
  
        config timeframe = 30d | datamodel dataset = msft_wsus_raw | sort desc _time | fields XDM.Endpoint.* | limit 20
