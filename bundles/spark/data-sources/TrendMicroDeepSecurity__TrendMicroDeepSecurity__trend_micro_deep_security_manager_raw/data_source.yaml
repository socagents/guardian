schema_version: 1
id: TrendMicroDeepSecurity
pack_name: TrendMicroDeepSecurity
rule_name: TrendMicroDeepSecurity
dataset_name: trend_micro_deep_security_manager_raw
vendor: Trend Micro
product: TrendMicroDeepSecurity
description: Trend Micro Deep Security provides runtime security for workloads (physical,
  virtual, cloud, and containers).
categories:
- Endpoint
version: 1.1.7
origin: bundle
author: phantom-bundle
uploaded_by: null
created_at: '2026-05-23T09:26:17Z'
updated_at: '2026-05-23T09:26:17Z'
logo:
  mime_type: image/svg+xml
  data: 
  source: phantom-bundle (v0.13.0 migration)
  license: MIT
  fidelity: branded
formats:
- SYSLOG
- CEF
- JSON
is_rawlog_only: false
fields:
- name: TrendMicroDsTags
  type: string
  description: Trend Micro Deep Security tags. Free-form text; may contain whitespace
    and punctuation.
  example: sample_trendmicrodstags
- name: TrendMicroDsTenant
  type: string
  description: Vendor-emitted field 'TrendMicroDsTenant'. Free-form text; may contain
    whitespace and punctuation.
  example: sample_trendmicrodstena
- name: TrendMicroDsTenantId
  type: string
  description: Vendor-emitted field 'TrendMicroDsTenantId'. Free-form text; may contain
    whitespace and punctuation.
  example: sample_trendmicrodstena
- name: cefDeviceEventClassId
  type: string_short
  description: CEF device-event class ID — vendor-specific event-type identifier.
    Short opaque token, typically without whitespace; max ~64 chars.
  example: token_value
- name: cefDeviceProduct
  type: string_short
  description: CEF device-product field — product name as registered by Trend Micro.
    Short opaque token, typically without whitespace; max ~64 chars.
  example: token_value
- name: cefDeviceVendor
  type: string_short
  description: CEF device-vendor field — vendor name. Short opaque token, typically
    without whitespace; max ~64 chars.
  example: token_value
- name: cefDeviceVersion
  type: string_short
  description: CEF device-version field — version of the Trend Micro product emitting
    this event. Short opaque token, typically without whitespace; max ~64 chars.
  example: token_value
- name: cefName
  type: string_short
  description: CEF event name — short human-readable label for the event class. Short
    opaque token, typically without whitespace; max ~64 chars.
  example: token_value
- name: cefSeverity
  type: integer
  description: CEF severity score (0-10) as classified by Trend Micro. Signed decimal
    integer; bare digits, optional leading minus.
  example: '42'
- name: cefVersion
  type: string_short
  description: CEF specification version — typically `0` for CEF 1.x. Short opaque
    token, typically without whitespace; max ~64 chars.
  example: token_value
- name: msg
  type: string
  description: Human-readable event message. Free-form text; may contain whitespace
    and punctuation.
  example: Operation completed successfully.
- name: src
  type: string
  description: CEF source IP — IPv4/IPv6 address of the source endpoint. Free-form
    text; may contain whitespace and punctuation.
  example: 192.0.2.45
- name: suser
  type: user
  description: CEF source user — username of the actor initiating the event. Account
    name as the vendor reports it — login name, UPN, or sAMAccountName; no domain
    prefix unless vendor emits one.
  example: jsmith
- name: target
  type: string
  description: Vendor-emitted field 'Target'. Free-form text; may contain whitespace
    and punctuation.
  example: target_object
- name: targetID
  type: string
  description: Vendor-emitted field 'TargetID'. Free-form text; may contain whitespace
    and punctuation.
  example: sample_targetid
- name: targetType
  type: string
  description: Vendor-emitted field 'TargetType'. Free-form text; may contain whitespace
    and punctuation.
  example: sample_targettype
use_cases:
- AV
- EDR
how_to_use: |
  ## Trend Micro TrendMicroDeepSecurity → Cortex XSIAM (dataset `trend_micro_deep_security_manager_raw`)
  
    **Routing** — the broker lowercases the CEF/syslog header `vendor`/`product`, replaces
    non-alphanumerics with `_`, and appends `_raw` to choose the dataset (target: `trend_micro_deep_security_manager_raw`).
  
    **Required CEF header for XSIAM** — the header must normalize to `trend_micro_deep_security_manager`. The marketplace pack's display name `Trend Micro` / `TrendMicroDeepSecurity` normalizes to `trend_micro_trendmicrodeepsecurity_raw`, a **different dataset** (vendor rename / legacy CEF identity), so use the vendor's actual CEF `deviceVendor`/`deviceProduct` that yields `trend_micro_deep_security_manager` and confirm which dataset the events land in.
  
    **Ingestion shape**: **Flat / CEF-compatible.** The modeling rule reads flat top-level columns (no nested-JSON access). A CEF event routes in and the broker auto-extracts those columns, so this source maps from Phantom's existing CEF path once the dataset is XDM-bound.
  
    - **Modeling-rule gate**: `cefDeviceProduct = "Deep Security Manager"` — seed that field with an accepted value or the rule drops the row (XDM stays 0).
  
    **XDM mapping**: ~62 fields mapped to the unified data model (`xdm.*`), including: `xdm.alert.category`, `xdm.alert.description`, `xdm.alert.mitre_techniques`, `xdm.alert.name`, `xdm.alert.original_threat_id`, `xdm.alert.severity`.
  
    **Operator setup**: This data source's modeling rule reads CEF-shaped columns (rt, src, dst, spt, dpt, cs1-6, act, etc.). Phantom emits CEF over syslog (`CEF:0|Trend Micro|TrendMicroDeepSecurity|...|<extensions>`) — the broker auto-extracts CEF k=v pairs into typed columns matching the CEF dictionary, and the broker routes to this dataset via the CEF header's vendor/product. Usually NO operator-side broker config is required beyond standard CEF ingestion.
  
    **Status**: Reverse-engineered 2026-06-04 from the parsing/modeling rules — routing, gate, and field
    inventory documented. **NOT yet XDM-validated on a live tenant** (no `validated` pill); validate by
    streaming a CEF batch and confirming a rich `xdm.*` count over a wide window.
  
    **Verify** (once the dataset is XDM-bound, wide ≥7d window):
  
        config timeframe = 30d | datamodel dataset = trend_micro_deep_security_manager_raw | sort desc _time | fields xdm.* | limit 20
