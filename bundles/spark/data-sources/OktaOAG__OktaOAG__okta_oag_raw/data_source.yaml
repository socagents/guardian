schema_version: 1
id: OktaOAG
pack_name: OktaOAG
rule_name: OktaOAG
dataset_name: okta_oag_raw
vendor: Okta
product: OktaOAG
description: Okta Access Gateway is a reverse proxy based virtual application, designed
  to secure web applications that don't natively support SAML or OIDC.
categories:
- IT Services
version: 1.0.10
origin: bundle
author: phantom-bundle
uploaded_by: null
created_at: '2026-05-24T16:53:58Z'
updated_at: '2026-05-24T16:53:58Z'
logo:
  mime_type: image/svg+xml
  data: 
  source: simpleicons:okta
  license: CC0-1.0
  fidelity: monochrome-brand
formats:
- SYSLOG
- CEF
- JSON
is_rawlog_only: false
fields:
- name: _raw_log
  type: string
  description: Raw log line as the vendor emits it before any parsing or normalization.
    Free-form text; may contain whitespace and punctuation.
  example: '<14>2024-01-15T14:23:01+0000 okta: oktaoag=OktaOAG action=allow user=jdoe'
- name: appHostname
  type: host
  description: Apphostname. Hostname or fully-qualified domain name (RFC 1035 dotted
    labels); no protocol prefix.
  example: host01.example.com
- name: appHostname2
  type: host
  description: Apphostname2. Hostname or fully-qualified domain name (RFC 1035 dotted
    labels); no protocol prefix.
  example: host01.example.com
- name: application
  type: string
  description: Application. Free-form text; may contain whitespace and punctuation.
  example: Salesforce
- name: auditedApp
  type: string
  description: Auditedapp. Free-form text; may contain whitespace and punctuation.
  example: sample_auditedapp
- name: clientIP
  type: ipv4
  description: Clientip. Dotted-quad IPv4 literal (e.g., 192.0.2.45); no port suffix,
    no surrounding brackets or quotes.
  example: 192.0.2.45
- name: component
  type: string
  description: Component. Free-form text; may contain whitespace and punctuation.
  example: auth-service
- name: description
  type: string
  description: Description. Free-form text; may contain whitespace and punctuation.
  example: Event recorded by Okta.
- name: event
  type: string
  description: Event. Free-form text; may contain whitespace and punctuation.
  example: login
- name: hostname
  type: host
  description: Hostname. Hostname or fully-qualified domain name (RFC 1035 dotted
    labels); no protocol prefix.
  example: host01.example.com
- name: logLevel
  type: string
  description: Loglevel. Free-form text; may contain whitespace and punctuation.
  example: sample_loglevel
- name: proc_id
  type: string_short
  description: Proc id. Short opaque token, typically without whitespace; max ~64
    chars.
  example: token
- name: reason
  type: string
  description: Reason. Free-form text; may contain whitespace and punctuation.
  example: normal_operation
- name: remoteIP
  type: ipv4
  description: Remoteip. Dotted-quad IPv4 literal (e.g., 192.0.2.45); no port suffix,
    no surrounding brackets or quotes.
  example: 192.0.2.45
- name: requestPayload
  type: string
  description: Requestpayload. Free-form text; may contain whitespace and punctuation.
  example: sample_requestpayload
- name: requestUrl
  type: url
  description: Requesturl. Full URL with scheme (http/https/ftp); percent-encoded
    if it contains non-ASCII characters.
  example: https://example.com/path?q=1
- name: result
  type: string
  description: Result. Free-form text; may contain whitespace and punctuation.
  example: success
- name: sessionID
  type: string
  description: Sessionid. Free-form text; may contain whitespace and punctuation.
  example: sess-dff146ab-ee9
- name: status
  type: string
  description: Status. Free-form text; may contain whitespace and punctuation.
  example: success
- name: subject
  type: string
  description: Subject. Free-form text; may contain whitespace and punctuation.
  example: Sample subject line
- name: userAgent
  type: string
  description: Useragent. Free-form text; may contain whitespace and punctuation.
  example: Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36
use_cases:
- Identity
- MFA
how_to_use: |
  ## okta oag → Cortex XSIAM (dataset `okta_oag_raw`)
  
    **Routing** — the broker lowercases the CEF/syslog header `vendor`/`product`, replaces
    non-alphanumerics with `_`, and appends `_raw` to choose the dataset (target: `okta_oag_raw`).
  
    **Required CEF header for XSIAM**:
    - **vendor**: `okta`
    - **product**: `oag`
  
    These normalize → `okta_oag_raw`.
  
    **Ingestion shape**: **Flat / CEF-compatible.** The modeling rule reads flat top-level columns (no nested-JSON access). A CEF event routes in and the broker auto-extracts those columns, so this source maps from Phantom's existing CEF path once the dataset is XDM-bound.
  
    - **Modeling-rule gate**: `_raw_log  contains "monitor"` — seed that field with an accepted value or the rule drops the row (XDM stays 0).
  
    **XDM mapping**: ~16 fields mapped to the unified data model (`xdm.*`), including: `xdm.alert.severity`, `xdm.event.description`, `xdm.event.operation`, `xdm.event.outcome`, `xdm.event.outcome_reason`, `xdm.network.http.method`.
  
    **Operator setup**: This data source uses `_raw_log`-based parsing. Cortex's parsing rule reads from `_raw_log` to populate `_json` (or directly to extract XDM fields), and the modeling rule does `regextract`/`json_extract_scalar` on `_raw_log`. For simulated logs sent from Phantom to land in this dataset, **the operator must add a Broker VM Syslog Applet pre-configured with the matching vendor + product + a dedicated source port**. Without that applet, logs are tagged `unknown/unknown` and routed to `unknown_unknown_raw` — the parsing rule never fires. 
  
  Setup: XSIAM → Settings → Configurations → Data Broker → Applets → Add Applet (Syslog) → Vendor = `okta`, Product = `oag`, Port = (operator picks an unused port, e.g. 1514, 1515, ...). Phantom must then send to that port — current default is 514, which is reserved for the ASA applet.
  
    **Status**: Reverse-engineered 2026-06-04 from the parsing/modeling rules — routing, gate, and field
    inventory documented. **NOT yet XDM-validated on a live tenant** (no `validated` pill); validate by
    streaming a CEF batch and confirming a rich `xdm.*` count over a wide window.
  
    **Verify** (once the dataset is XDM-bound, wide ≥7d window):
  
        config timeframe = 30d | datamodel dataset = okta_oag_raw | sort desc _time | fields xdm.* | limit 20
