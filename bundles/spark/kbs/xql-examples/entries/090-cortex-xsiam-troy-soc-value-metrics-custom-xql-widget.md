---
id: XQL-090-9fb16aaa
title: Cortex XSIAM Troy SOC Value Metrics - Custom XQL Widget
category: investigation
dataset: alerts
tags:
  - arrayexpand
  - comp
  - filter
  - sort
  - view
---

# Cortex XSIAM Troy SOC Value Metrics - Custom XQL Widget

**Dataset**: `alerts`

```sql
dataset = alerts
| arrayexpand original_tags
| filter original_tags contains "DS:"
// Total Incidents
| comp count_distinct(incident_id) as total_incidents by original_tags
| sort desc total_incidents
| view graph type = pie header = "Total Incidents" xaxis = original_tags yaxis = total_incidents
```

## When to use

Cortex XSIAM Troy SOC Value Metrics - Custom XQL Widget. Queries the `alerts` dataset directly, filtered on `original_tags contains "DS:"`. Uses stages: `arrayexpand`, `filter`, `comp`, `sort`, `view`.

## Variations

_(Backfilled — variations not yet authored. The operator's curation pass adds these.)_

## Source

Backfilled from the pre-v0.6.51 KB seed corpus (`bundles/spark/kbs/xql-examples/seed/xql_examples.md`). The `When to use` description above was auto-generated by the backfill heuristic — operator-curation pass pending. The query body is the authoritative version regardless of description quality. Schema-uniformity backfill landed in v0.6.52.
