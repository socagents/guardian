---
id: XQL-072-1800f63a
title: Cortex XSIAM Troy Traffic Overview - Custom XQL Widget
category: investigation
tags:
  - alter
  - comp
  - sort
  - view
---

# Cortex XSIAM Troy Traffic Overview - Custom XQL Widget

**Dataset**: `(none)`

```sql
preset = network_story
| alter proto = to_string(action_network_protocol )
| alter proto = if(proto = "1", "ICMP",proto = "6", "TCP",proto = "17", "UDP", proto = "47", "GRE", proto = "4", "IP-in-IP")
| comp count(event_id ) as traffic_count by proto
| sort desc traffic_count
| view graph type = pie subtype = semi_donut xaxis = proto yaxis = traffic_count
```

## When to use

Cortex XSIAM Troy Traffic Overview - Custom XQL Widget. Queries the `(none)` preset. Uses stages: `alter`, `comp`, `sort`, `view`.

## Variations

_(Backfilled — variations not yet authored. The operator's curation pass adds these.)_

## Source

Backfilled from the pre-v0.6.51 KB seed corpus (`bundles/spark/kbs/xql-examples/seed/xql_examples.md`). The `When to use` description above was auto-generated by the backfill heuristic — operator-curation pass pending. The query body is the authoritative version regardless of description quality. Schema-uniformity backfill landed in v0.6.52.
