---
id: XQL-063-46e8257b
title: Cortex XSIAM Troy NGFW Threat Hunting - Custom XQL Widget
category: investigation
dataset: panw_ngfw_traffic_raw
tags:
  - comp
  - fields
  - filter
  - sort
  - view
---

# Cortex XSIAM Troy NGFW Threat Hunting - Custom XQL Widget

**Dataset**: `panw_ngfw_traffic_raw`

```sql
dataset = panw_ngfw_traffic_raw
| filter app not contains "not-applicable" and app not contains "incomplete"
| fields app
| comp count(app) as app_num by app
| sort desc app_num
| view graph type = pie xaxis = app yaxis = app_num
```

## When to use

Cortex XSIAM Troy NGFW Threat Hunting - Custom XQL Widget. Queries the `panw_ngfw_traffic_raw` dataset directly, filtered on `app not contains "not-applicable" and app not contains "incomplete"`. Uses stages: `filter`, `fields`, `comp`, `sort`, `view`.

## Variations

_(Backfilled — variations not yet authored. The operator's curation pass adds these.)_

## Source

Backfilled from the pre-v0.6.51 KB seed corpus (`bundles/spark/kbs/xql-examples/seed/xql_examples.md`). The `When to use` description above was auto-generated by the backfill heuristic — operator-curation pass pending. The query body is the authoritative version regardless of description quality. Schema-uniformity backfill landed in v0.6.52.
