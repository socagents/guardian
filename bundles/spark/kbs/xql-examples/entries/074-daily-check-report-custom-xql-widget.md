---
id: XQL-074-e5ae180e
title: Daily Check Report - Custom XQL Widget
category: investigation
dataset: panw_ngfw_system_raw
tags:
  - fields
  - filter
  - sort
---

# Daily Check Report - Custom XQL Widget

**Dataset**: `panw_ngfw_system_raw`

```sql
dataset = panw_ngfw_system_raw
| filter severity in ("high", "critical") and _reporting_device_name in ("BH_ASIA_*")
| fields _reporting_device_name, log_type, severity, event_name, event_description
| sort desc _time
```

## When to use

Daily Check Report - Custom XQL Widget. Queries the `panw_ngfw_system_raw` dataset directly, filtered on `severity in ("high", "critical") and _reporting_device_name in ("BH_ASIA_*")`. Uses stages: `filter`, `fields`, `sort`.

## Variations

_(Backfilled — variations not yet authored. The operator's curation pass adds these.)_

## Source

Backfilled from the pre-v0.6.51 KB seed corpus (`bundles/spark/kbs/xql-examples/seed/xql_examples.md`). The `When to use` description above was auto-generated by the backfill heuristic — operator-curation pass pending. The query body is the authoritative version regardless of description quality. Schema-uniformity backfill landed in v0.6.52.
