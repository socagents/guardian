---
id: XQL-106-1fef4d0f
title: Cortex XSIAM Troy Geo Locations - Custom XQL Widget
category: investigation
tags:
  - comp
  - filter
  - iploc
  - limit
  - sort
  - union
  - view
---

# Cortex XSIAM Troy Geo Locations - Custom XQL Widget

**Dataset**: `(none)`

```sql
preset = network_story
| iploc action_remote_ip loc_country
| filter loc_country != null
| union (preset = network_story| iploc action_local_ip loc_country | filter loc_country != null)
| comp count(event_id) as counter by loc_country
| sort desc counter
| limit 10
| view graph type = pie subtype = semi_donut xaxis = loc_country yaxis = counter
```

## When to use

Cortex XSIAM Troy Geo Locations - Custom XQL Widget. Queries the `(none)` preset, filtered on `loc_country != null`. Uses stages: `iploc`, `filter`, `union`, `comp`, `sort`, `limit`.

## Variations

_(Backfilled — variations not yet authored. The operator's curation pass adds these.)_

## Source

Backfilled from the pre-v0.6.51 KB seed corpus (`bundles/spark/kbs/xql-examples/seed/xql_examples.md`). The `When to use` description above was auto-generated by the backfill heuristic — operator-curation pass pending. The query body is the authoritative version regardless of description quality. Schema-uniformity backfill landed in v0.6.52.
