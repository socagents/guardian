---
id: XQL-104-ee730375
title: Cortex XSIAM Troy DNS Activity - Custom XQL Widget
category: investigation
tags:
  - alter
  - comp
  - filter
  - sort
  - view
---

# Cortex XSIAM Troy DNS Activity - Custom XQL Widget

**Dataset**: `(none)`

```sql
preset = network_story
| alter app = lowercase(arraystring(action_app_id_transitions,","))
| filter app contains "dns"
| comp count(event_id ) as counter by dns_reply_code
| sort desc counter
| view graph type = column subtype = grouped layout = horizontal show_callouts = `true` xaxis = dns_reply_code yaxis = counter seriescolor("counter","#ec9d7c") xvaluesfontsize = 15 calloutfontsize = 15 legend = `false`
```

## When to use

Cortex XSIAM Troy DNS Activity - Custom XQL Widget. Queries the `(none)` preset, filtered on `app contains "dns"`. Uses stages: `alter`, `filter`, `comp`, `sort`, `view`.

## Variations

_(Backfilled — variations not yet authored. The operator's curation pass adds these.)_

## Source

Backfilled from the pre-v0.6.51 KB seed corpus (`bundles/spark/kbs/xql-examples/seed/xql_examples.md`). The `When to use` description above was auto-generated by the backfill heuristic — operator-curation pass pending. The query body is the authoritative version regardless of description quality. Schema-uniformity backfill landed in v0.6.52.
