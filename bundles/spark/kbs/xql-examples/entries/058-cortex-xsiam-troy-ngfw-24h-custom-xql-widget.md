---
id: XQL-058-423f4558
title: Cortex XSIAM Troy NGFW 24h - Custom XQL Widget
category: investigation
dataset: incidents
tags:
  - comp
  - filter
  - view
---

# Cortex XSIAM Troy NGFW 24h - Custom XQL Widget

**Dataset**: `incidents`

```sql
dataset = incidents
| filter status = ENUM.RESOLVED_AUTO_RESOLVE and alert_sources = "FW"
| comp count(status)
| view graph type = single subtype = standard header = "Auto Closed 24h" yaxis = count_1 headcolor = "#08ff4b" headerfontsize = 60
```

## When to use

Cortex XSIAM Troy NGFW 24h - Custom XQL Widget. Queries the `incidents` dataset directly, filtered on `status = ENUM.RESOLVED_AUTO_RESOLVE and alert_sources = "FW"`. Uses stages: `filter`, `comp`, `view`.

## Variations

_(Backfilled — variations not yet authored. The operator's curation pass adds these.)_

## Source

Backfilled from the pre-v0.6.51 KB seed corpus (`bundles/spark/kbs/xql-examples/seed/xql_examples.md`). The `When to use` description above was auto-generated by the backfill heuristic — operator-curation pass pending. The query body is the authoritative version regardless of description quality. Schema-uniformity backfill landed in v0.6.52.
