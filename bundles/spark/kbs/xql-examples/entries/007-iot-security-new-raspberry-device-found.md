---
id: XQL-007-94cde9af
title: IoT Security - New Raspberry Device Found
category: investigation
dataset: panw_iot_security_devices_raw
tags:
  - alter
  - dedup
  - filter
---

# IoT Security - New Raspberry Device Found

**Dataset**: `panw_iot_security_devices_raw`

```sql
dataset = panw_iot_security_devices_raw
| filter profile contains "Raspberry Pi Device"
| alter duration_time = timestamp_diff(_insert_time,first_seen_date,"MINUTE")
| filter duration_time <= 30 and duration_time  >= 0
//| dedup mac_address
```

## When to use

IoT Security - New Raspberry Device Found. Queries the `panw_iot_security_devices_raw` dataset directly, filtered on `profile contains "Raspberry Pi Device"`. Uses stages: `filter`, `alter`, `dedup`.

## Variations

_(Backfilled — variations not yet authored. The operator's curation pass adds these.)_

## Source

Backfilled from the pre-v0.6.51 KB seed corpus (`bundles/spark/kbs/xql-examples/seed/xql_examples.md`). The `When to use` description above was auto-generated by the backfill heuristic — operator-curation pass pending. The query body is the authoritative version regardless of description quality. Schema-uniformity backfill landed in v0.6.52.
