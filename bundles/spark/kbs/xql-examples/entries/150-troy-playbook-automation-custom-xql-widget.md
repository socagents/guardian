---
id: XQL-150-0170f0b5
title: Troy Playbook Automation - Custom XQL Widget
category: investigation
dataset: xsiam_playbookmetrics_raw
tags:
  - alter
  - comp
  - dedup
  - view
---

# Troy Playbook Automation - Custom XQL Widget

**Dataset**: `xsiam_playbookmetrics_raw`

```sql
dataset = xsiam_playbookmetrics_raw
| alter playbookId = alert->playbookId
| alter alertId = alert->id
| dedup alertId
| comp count(playbookId)
| view graph type = single subtype = standard header = "Playbook Runs" yaxis = count_1
```

## When to use

Troy Playbook Automation - Custom XQL Widget. Queries the `xsiam_playbookmetrics_raw` dataset directly. Uses stages: `alter`, `dedup`, `comp`, `view`.

## Variations

_(Backfilled — variations not yet authored. The operator's curation pass adds these.)_

## Source

Backfilled from the pre-v0.6.51 KB seed corpus (`bundles/spark/kbs/xql-examples/seed/xql_examples.md`). The `When to use` description above was auto-generated by the backfill heuristic — operator-curation pass pending. The query body is the authoritative version regardless of description quality. Schema-uniformity backfill landed in v0.6.52.
